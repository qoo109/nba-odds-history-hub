# V0.21 scope note

This branch implements only the next `PROJECT_STATUS.md` mainline:

```text
OFFSEASON_PRESEASON_DISABLED_IMPORT_REQUEST_CONTRACT_AND_BACKUP_RESTORE_FIXTURES
```

It is intentionally non-executable. It binds a committed synthetic fixture identity and validates backup/restore against a temporary SQLite database outside the repository.

Not included:

- owner approval request or decision;
- production database path or backup identifier;
- external schedule retrieval;
- production schedule import;
- recurring collection;
- canonical event ID creation;
- odds capture;
- runnable shell command;
- production import implementation;
- writes to `qoo109/nba-value-lab`.
